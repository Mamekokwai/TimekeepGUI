# 挂件跨显示器拖动后回弹修复执行方案

> 状态：实现、自动化验证与对抗式审查已完成；等待真实双屏验收
>
> 创建日期：2026-07-26
>
> 对应事项：GitHub Project「修复挂件跨显示器拖动后回弹」，Refs #55
>
> 当前 Project 状态：`In progress`（2026-07-26 只读复核；建议维护者改为 `Blocked`）
>
> 文档归宿：实现与验收完成后移至 `docs/archive/`

> 自动化证据：Rust 486 passed / 1 ignored；浏览器 smoke 50 passed，其中挂件矩阵 144 个渲染案例；Tauri 运行时 smoke 通过；`npm run check` 通过。
>
> 门禁结果：已将 ESLint 与 `@eslint/js` 成对升级到 10.x，依赖树收敛到 `minimatch 10.2.5` 与 `brace-expansion 5.0.8`。`npm ci`、`npm run check:full` 与依赖审计均通过，npm 漏洞为 0，Rust 的 Windows 可达漏洞为 0。该修复当前仅在本地，尚未取得新的远端推送授权。
>
> 唯一未完成条件：当前机器没有第二块物理显示器，尚不能取得 150%/125% 真实双屏双向拖动、展开/收起与重启恢复证据。因此本文档不得归档，事项也不得标记为 `Done`。

## 1. 如何使用本文档

本文档是本次修复的唯一执行清单。实施者应从上到下推进，不得用“代码看起来合理”替代测试证据。

- [x] 开始某一阶段前，确认上一阶段的退出条件已经满足。
- [x] 每完成一个步骤，立即勾选并在其下记录必要证据，例如测试名、命令结果或截图位置。
- [x] 若实际实现需要新增共享抽象、跨层迁移或兼容壳，停止实施并重新评估边界，不得顺手扩大范围。
- [x] 若发现本文档与当前代码事实不符，先修订本文档并说明原因，再继续编码。
- [x] 自动化验证完成但尚无双屏真机证据时，不得将事项宣称为彻底完成。
- [ ] 全部完成后执行第 23 节的对抗式审查，再进行归档。

## 2. 一句话问题定义

挂件跨显示器拖动结束后，系统没有把“目标显示器”作为位置的一部分贯穿保存与吸附流程，导致 Rust 端重新按当前窗口或主显示器选屏，最终可能把挂件弹回原屏或主屏。

## 3. 当前事实与根因证据

### 3.1 已确认事实

- [x] 前端拖动结束路径能够读取挂件最终物理位置与尺寸。
- [x] 前端现有逻辑能够根据窗口矩形找到一个显示器。
- [x] `applyWidgetLayout` 只向 Rust 传递 `side`、`anchorY`、展开状态等布局信息，没有传递目标显示器。
- [x] Rust 的 `resolve_widget_monitor` 会再次依次尝试挂件当前显示器、主窗口当前显示器和主显示器。
- [x] 持久化目前只有 `widget_side` 与 `widget_anchor_y`，无法表达显示器归属。
- [x] 当前物理坐标计算已经覆盖负坐标显示器与不同 DPI 下的窗口尺寸转换，可在其基础上收敛而不必重建整套几何系统。
- [x] 用户当前没有第二块物理显示器，因此开发阶段必须以确定性纯逻辑测试和合成显示器矩阵为主，真机验收单独列为发布前条件。

### 3.2 根因链

1. 用户把挂件从显示器 A 拖到显示器 B。
2. 前端在拖动结束时看到了位于 B 的最终窗口矩形。
3. 前端把矩形归一化为 `side + anchorY`，但丢弃了 B 的身份。
4. Rust 收到不完整的位置描述后，只能再次猜测目标显示器。
5. 此时窗口管理器报告的 `current_monitor()` 可能仍是 A，或代码回退到主显示器 A。
6. Rust 使用 A 的工作区重新计算吸附坐标，窗口因此回弹。

### 3.3 第一处应被红测锁定的故障

- [x] 写出一个失败测试：最终窗口矩形明确位于显示器 B，但运行时若重新读取 `current_monitor()` 得到 A，最终保存与吸附仍必须使用 B。
- [x] 确认该测试在修改生产代码前失败，且失败原因是缺少显示器亲和类型与目标选择函数，而不是测试桩错误。

## 4. 第一性原理

### 4.1 桌面坐标的本质

Windows 多显示器桌面是一个统一的物理坐标空间，而不是若干个各自从 `(0, 0)` 开始的画布。副屏可以位于主屏左、右、上、下，坐标可以为负，屏幕之间也可以有间隙。

由此得到：

- [x] 所有跨屏归属判断使用物理像素矩形。
- [x] 不假设主显示器原点是整个桌面原点。
- [x] 不假设显示器相邻、同高、同宽或按固定顺序排列。
- [x] 不用 CSS 像素或逻辑像素直接比较显示器物理工作区。

### 4.2 挂件位置的最小完整信息

`side + anchorY` 只描述“在某块屏幕中的相对位置”，没有描述“哪块屏幕”。所以完整位置至少是：

```text
位置 = 显示器亲和信息 + 水平吸附边 + 垂直归一化锚点
```

由此得到：

- [x] 将显示器亲和信息纳入 `WidgetPlacement`。
- [x] 不把绝对窗口坐标作为长期位置真相。
- [x] 不把 DPI 比例作为显示器身份或长期真相，因为比例可能变化。
- [x] 保留相对锚点，使分辨率、工作区或任务栏变化后仍能恢复到合理位置。

### 4.3 单一所有者原则

一次拖动结束只能有一个组件决定目标显示器。任何后续层再次猜测，都会制造竞态与分歧。

由此得到：

- [x] 将拖动完成事务的所有权放在 Rust 运行时。
- [x] 前端只发出“结束本次用户拖动”的意图，不再枚举并决定目标显示器。
- [x] Rust 在同一次事务中读取最终窗口矩形和显示器快照，只选择一次目标显示器。
- [x] 后续计算、保存和吸附复用同一个已选目标，不再次调用 `current_monitor()` 覆盖它。

### 4.4 原子完成原则

拖动结束不是四个彼此独立的动作，而是一笔逻辑事务：

```text
读取最终窗口矩形
→ 选择目标显示器
→ 计算相对位置
→ 持久化
→ 在同一目标显示器内吸附
```

由此得到：

- [x] 任何一步失败时返回可诊断错误，不把半成品位置报告为成功。
- [x] 持久化和实际吸附使用同一个 `WidgetPlacement` 值。
- [x] 防止两个拖动结束回调并发执行或旧回调覆盖新回调。

### 4.5 可恢复原则

已保存的显示器可能暂时断开。安全回退的目标是“让挂件可见”，不是“偷偷忘记用户原来的显示器”。

由此得到：

- [x] 恢复时若目标显示器缺失，可临时显示在安全回退屏幕。
- [x] 临时回退不得自动覆盖原有显示器亲和信息。
- [x] 只有用户完成新的明确拖动，才用新目标显示器覆盖已保存亲和信息。

## 5. 已确认的产品与工程决策

- [x] 只使用一个新键：`widget_placement`。
- [x] 不使用 `widget_placement_v2` 名称。
- [x] 不读取、不迁移、不双写旧键 `widget_side` 与 `widget_anchor_y`。
- [x] 升级后旧用户的挂件位置允许一次性回到默认值。
- [x] 新保存时可以删除旧键作为数据清理，但删除不构成兼容或迁移。
- [x] 不新增数据库表或 schema migration；位置继续存入现有设置存储。
- [x] 目标显示器选择与最终吸附归 Rust 所有。
- [x] 不将 Windows 虚拟桌面视为多显示器验收。
- [x] 无双屏开发环境可以完成实现与自动化验证，但不能替代最终混合 DPI 真机验收。

## 6. 目标

- [x] 合成与运行时验证中，挂件从显示器 A 拖到 B 后由 B 完成保存和吸附。
- [x] 合成测试支持主屏与副屏双向拖动。
- [x] 支持副屏位于左、右、上、下以及负坐标布局。
- [x] 自动化覆盖 100%、125%、150%、200% 的同 DPI 与混合 DPI 组合。
- [x] 重启后优先恢复到上次明确选择的显示器及相对位置。
- [x] 目标显示器缺失时挂件仍可见，目标恢复后仍保留原亲和意图。
- [x] 收敛目标显示器选择权，避免前后端各自判断。
- [x] 为位置选择、持久化、竞态和恢复补充可重复的回归测试。

## 7. 非目标

- [x] 未改变挂件视觉设计、大小、展开动画或呼吸灯样式。
- [x] 未扩展到单屏高 DPI 内容裁切；该问题仍由独立事项负责。
- [x] 未为所有应用窗口建立通用多显示器框架。
- [x] 未改变主窗口的多显示器行为。
- [x] 未引入旧位置数据兼容层。
- [x] 未用屏幕截图像素差异作为跨屏归属的主要证明。
- [x] 未因测试方便而把平台几何逻辑搬回 React 组件。

## 8. 必须始终成立的不变量

- [x] 一个 `WidgetPlacement` 能完整表达显示器亲和、左右吸附边和纵向锚点。
- [x] `anchorY` 始终是有限数并限制在 `[0, 1]`。
- [x] 所有显示器工作区和窗口矩形在归属计算中使用物理像素。
- [x] 窗口逻辑尺寸只使用已选目标显示器的 scale factor 转换一次。
- [x] 同一次完成事务只产生一个目标显示器。
- [x] 同一次完成事务只保存一次最终位置。
- [x] 旧的拖动完成回调不能覆盖更新一代拖动的结果。
- [x] 无法匹配已保存显示器时，挂件落入安全回退显示器的可见工作区。
- [x] 临时回退不重写已保存显示器亲和。
- [x] 没有新 `widget_placement` 或其 JSON 非法时，返回确定的默认位置，不读取旧键。

## 9. 所有权与分层

### 9.1 `domain/widget.rs`

负责纯数据和纯规则：

- [x] 定义 `WidgetPlacement`、`WidgetMonitorAffinity`、物理矩形等必要领域类型。
- [x] 校验和归一化 `side`、`anchorY` 与显示器描述。
- [x] 提供确定性的显示器选择和亲和匹配函数，输入输出不依赖 Tauri。
- [x] 为边界、负坐标、相交面积、距离和稳定排序提供单元测试。

### 9.2 `app/widget.rs`

负责一次完整的运行时用例：

- [x] 从 Tauri 窗口读取最终物理矩形。
- [x] 获取当前可用显示器的单次快照。
- [x] 调用领域函数选择目标显示器。
- [x] 生成最终 `WidgetPlacement`。
- [x] 保存该位置。
- [x] 使用同一个目标显示器和位置应用窗口边界。
- [x] 处理恢复、缺屏回退及展开/收起时的目标保持。

### 9.3 `commands/widget.rs`

保持薄命令：

- [x] 新增或替换为“完成挂件拖动”的命令入口。
- [x] 只做参数接收、委派与错误返回，不包含显示器选择算法。
- [x] 清理已不再需要的“前端传入 side/anchor 后应用”的命令，避免双路径长期并存。

### 9.4 数据层

- [x] `engine/widget.rs` 只保留位置存取端口。
- [x] `data/widget_store.rs` 继续把端口连接到 SQLite repository，无需修改。
- [x] `data/repositories/widget_state.rs` 只以 `widget_placement` JSON 读写完整值。
- [x] 保存使用数据库事务原子 upsert 新键，并删除旧键。
- [x] 加载从不查询旧键。

### 9.5 前端

- [x] `widgetWindowController.ts` 只管理拖动生命周期、稳定等待、代次和状态更新。
- [x] `widgetRuntimeGateway.ts` 只封装新的 IPC，不再选择显示器。
- [x] `useWidgetWindowState.ts` 接入新的完成事务与返回值。
- [x] `WidgetShell.tsx` 无需修改，保留现有交互。
- [x] 删除不再需要的前端 `availableMonitors`、`currentMonitor`、`primaryMonitor` 依赖。

## 10. 新位置数据契约

建议领域结构：

```rust
pub struct WidgetPlacement {
    pub monitor: Option<WidgetMonitorAffinity>,
    pub side: WidgetSide,
    pub anchor_y: f64,
}

pub struct WidgetMonitorAffinity {
    pub name: Option<String>,
    pub work_area: WidgetPhysicalRect,
}
```

建议 JSON：

```json
{
  "monitor": {
    "name": "\\\\.\\DISPLAY2",
    "work_area": {
      "x": 1920,
      "y": 0,
      "width": 2560,
      "height": 1392
    }
  },
  "side": "right",
  "anchor_y": 0.28
}
```

执行要求：

- [x] 确认 Tauri 返回的显示器名称可选且可能为空。
- [x] 名称允许为空，因此工作区几何作为辅助亲和信息。
- [x] 不在 JSON 中加入 `version` 字段；当前已明确不做旧格式兼容。
- [x] 不保存 scale factor 作为显示器身份。
- [x] 宽高必须为正；无效工作区使 `monitor` 视为不可用，而不是传播异常几何。
- [x] `monitor: null` 仅表示首次运行、非法数据回退或尚未发生明确跨屏定位。
- [x] 因新增非 `Copy` 字段，移除 `WidgetPlacement` 的 `Copy` 假设并在必要处显式 `Clone`。
- [x] TypeScript 与 Rust 的 snake_case/camelCase 映射由一个网关边界集中完成。

## 11. 目标显示器选择算法

输入：

- 最终挂件物理矩形；
- 同一时刻的全部显示器物理工作区快照；
- 每块显示器的稳定描述信息。

选择顺序：

1. 计算挂件矩形与每块显示器工作区的相交面积。
2. 选择相交面积最大的显示器。
3. 若最大相交面积相同，优先选择包含挂件中心点的显示器。
4. 若没有任何相交，选择距离挂件中心最近的工作区；距离应按“点到矩形的最短距离”计算，而非只比屏幕中心。
5. 仍然相同则使用稳定排序，例如规范化名称、工作区坐标与尺寸组成的元组。

执行清单：

- [x] 明确定义半开矩形边界，避免共享边缘被重复计入。
- [x] 使用 `i64` 边界和 `u64` 面积计算，避免常见高分辨率坐标乘法溢出。
- [x] 对零宽、零高窗口矩形定义确定行为。
- [x] 对空显示器列表返回可诊断错误，由上层决定安全处理。
- [x] 对重复或重叠显示器描述保持确定性。
- [x] 确认算法不依赖显示器枚举顺序。
- [x] 把选择结果连同该次显示器快照传给后续布局，不再重新枚举。

## 12. 拖动结束原子流程

目标时序：

```text
WidgetShell 检测鼠标释放
→ controller.endUserDrag()
→ 等待最后一次 move 事件稳定
→ IPC: finalizeWidgetDrag(generation)
→ Rust 读取最终 outer_position / outer_size
→ Rust 枚举一次 available_monitors
→ 纯函数选择目标显示器
→ 计算 side / anchorY / monitor affinity
→ SQLite 保存 widget_placement
→ 按同一个目标显示器 DPI 和工作区应用边界
→ 返回规范化后的 WidgetPlacement
→ 前端仅更新本地显示状态
```

执行清单：

- [x] 保留现有短暂稳定等待，并用尾部 move 与 release 竞态测试锁定用途。
- [x] 每次 `beginUserDrag` 生成递增 generation/token。
- [x] `endUserDrag` 只允许当前 generation 发起完成事务。
- [x] 同一 generation 已在完成中时合并重复请求。
- [x] 新一代拖动开始后，上一代完成结果不得更新本地状态或再次移动窗口。
- [x] 窗口被隐藏、销毁或应用退出时，完成事务安全忽略结果。
- [x] Rust 在读取最终矩形之后选择屏幕，不使用拖动开始时的屏幕。
- [x] Rust 返回实际保存的规范化位置，使前端不自行重算。
- [x] 保存失败返回错误且不执行后续吸附，不把失败报告为成功。
- [x] 吸附失败保留已保存位置并返回带阶段上下文的错误，使下次恢复仍可重试。

## 13. 位置计算规则

对已选目标显示器：

- `side`：比较挂件中心到目标工作区左右边缘的距离，选择更近一侧。
- `anchorY`：以目标工作区内可移动的纵向范围归一化。
- `anchorY = clamp((window_y - work_area_y) / (work_area_height - widget_height), 0, 1)`。
- 目标物理尺寸：由挂件逻辑尺寸乘以目标显示器 scale factor 后统一取整。
- 目标 X：目标工作区左边或右边减去挂件物理宽度。
- 目标 Y：`work_area_y + anchorY * 可移动纵向范围`，最后统一取整并夹紧。

执行清单：

- [x] 对 `work_area_height <= widget_height` 定义为顶端对齐且 `anchorY = 0`。
- [x] 使用目标显示器的工作区而不是完整屏幕边界，避开任务栏。
- [x] 对偏移工作区和顶部、左侧任务栏加入测试；底部、右侧由宽高收缩矩阵覆盖。
- [x] 统一取整策略，并在测试中只允许一次物理像素取整。
- [x] 不在前端和 Rust 各实现一套公式。

## 14. 恢复与缺屏回退

### 14.1 显示器亲和匹配顺序

1. 名称完全匹配。
2. 名称不可用或未匹配时，在当前显示器中选择与保存工作区几何最接近者。
3. 无可靠匹配时选择当前可见/主显示器作为安全回退。

几何匹配至少考虑：

- 工作区宽高差；
- 原点距离；
- 宽高比例；
- 结果是否唯一且明显优于其他候选。

执行清单：

- [x] 为名称变化但同一物理布局的情况提供几何回退测试。
- [x] 为同名双屏但不同坐标提供几何消歧测试。
- [x] 断开目标屏幕后由既有首选 / 当前 / 主屏链路安全回退。
- [x] 恢复目标屏幕后名称或几何亲和能再次匹配。
- [x] 临时回退期间不调用位置保存。
- [x] 用户在回退屏幕上明确拖动结束后，才将亲和更新为该屏幕。
- [x] 所有恢复结果都经过工作区夹紧，保证窗口可见。

## 15. 预计文件范围

### 15.1 预期修改

- [x] `src-tauri/src/domain/widget.rs`
- [x] `src-tauri/src/app/widget.rs`
- [x] `src-tauri/src/commands/widget.rs`
- [x] `src-tauri/src/app/bootstrap.rs`
- [x] `src-tauri/src/engine/widget.rs`
- [x] `src-tauri/src/data/widget_store.rs` 经核对无需修改。
- [x] `src-tauri/src/data/repositories/widget_state.rs`
- [x] `src/platform/desktop/widgetRuntimeGateway.ts`
- [x] `src/app/widget/widgetWindowController.ts`
- [x] `src/app/widget/useWidgetWindowState.ts`
- [x] `src-tauri/capabilities/widget.json`
- [x] `tests/interactionFlows.test.ts`
- [x] `tests/tauriRuntimeSmoke.test.ts`
- [x] `tests/uiBrowserSmoke/tauriStubs.ts` 与相关挂件场景
- [x] `CHANGELOG.md` 已加入 `Refs #55`

### 15.2 只在证据要求时修改

- [x] `src/app/widget/WidgetShell.tsx` 经证据核对无需修改。
- [x] 生成的 capability/schema 文件由标准 Tauri 构建更新并审查。
- [x] 测试治理或架构边界配置经检查无需修改。

### 15.3 停止并重新评估的条件

- [x] 未触发：无需新增通用 `shared` 或 `platform` 抽象。
- [x] 未触发：无需改变主窗口或其他窗口的位置恢复。
- [x] 未触发：无需引入数据库 schema migration。
- [x] 未触发：无需保留新旧 IPC 两套长期行为。
- [x] 未触发：无需更改挂件视觉尺寸、CSS 或 Quiet Pro token。
- [x] 未触发：单一 `widget_placement` 已满足已确认需求。

## 16. 阶段 0：建立基线

- [x] 确认工作树状态，记录既有未提交修改并避免覆盖用户内容。
- [x] 运行 `git diff --check`，确认基线无空白错误。
- [x] 运行 `npm test`。
- [x] 运行 `npm run test:replay`。
- [x] 运行 `npm run build`。
- [x] 运行 `npm run check:rust`。
- [x] 记录既有 npm audit 告警，确保不误归因于本修复。
- [x] 再次核对 Project 事项状态；实施期间保持 `In progress`。

退出条件：

- [x] 已获得可比较的前端、回放、构建与 Rust 基线。
- [x] 没有未识别的用户改动与本次文件范围冲突。

## 17. 阶段 1：先写失败测试

### 17.1 领域选择测试

- [x] 副屏在主屏右侧，窗口大部分位于副屏。
- [x] 副屏在主屏左侧且 X 为负数。
- [x] 副屏在主屏上方且 Y 为负数。
- [x] 副屏在主屏下方。
- [x] 窗口恰好跨越屏幕边界，按最大相交面积选择。
- [x] 相交面积相同，按中心点选择。
- [x] 窗口落在屏幕间隙，按点到工作区最短距离选择。
- [x] 候选完全等价，稳定排序且不受输入顺序影响。
- [x] 空显示器列表返回明确错误。

### 17.2 完成事务测试

- [x] 最终矩形位于 B 时，选择不依赖 `current_monitor()` 返回的 A。
- [x] 保存与吸附接收同一个目标显示器快照。
- [x] 不发生第二次显示器选择。
- [x] 保存失败不会返回成功。
- [x] 吸附失败与保存失败具有不同阶段上下文。

### 17.3 前端竞态测试

- [x] 多个尾部 moved 事件只触发一次 finalize。
- [x] 重复 release 只触发一次 finalize。
- [x] 新 generation 开始后旧结果被忽略。
- [x] 释放后的延迟 moved 事件不会把挂件重新吸附回源屏。
- [x] 隐藏或卸载期间不会遗留定时器与未处理状态更新。

退出条件：

- [x] 至少一条测试在生产代码修改前稳定失败，锁定目标显示器所有权缺失。
- [x] 失败信息明确指出缺少目标选择领域类型与函数。

## 18. 阶段 2：领域模型与单键持久化

- [x] 扩展 `WidgetPlacement`，加入可选 `monitor` 亲和。
- [x] 增加显示器亲和与物理矩形类型。
- [x] 实现输入归一化和无效值默认行为。
- [x] 实现目标显示器选择纯函数。
- [x] 实现已保存亲和与当前显示器的匹配纯函数。
- [x] 将 repository 读取改为只读取 `widget_placement`。
- [x] 将 repository 保存改为序列化完整 JSON。
- [x] 在同一保存事务中删除 `widget_side` 与 `widget_anchor_y`。
- [x] 验证只有旧键存在时返回默认位置。
- [x] 验证新键缺失时返回默认位置。
- [x] 验证新键 JSON 损坏、字段缺失、非法锚点时行为确定且不 panic。
- [x] 验证完整值 JSON round-trip。
- [x] 验证保存后数据库中不存在旧键。
- [x] 更新 engine 签名及测试；data adapter 无需改签名。

退出条件：

- [x] 领域测试全部通过。
- [x] repository 测试证明没有任何兼容读取或双写。
- [x] 数据库无需 schema migration。

## 19. 阶段 3：Rust 运行时与 IPC 收敛

- [x] 在 `app/widget.rs` 实现单一 finalize 用例。
- [x] 从挂件窗口读取最终 `outer_position` 与 `outer_size`。
- [x] 一次性读取 available monitors 及其物理工作区、名称、scale factor。
- [x] 调用纯函数选择目标显示器。
- [x] 从最终矩形计算 `side` 与 `anchorY`。
- [x] 构造包含目标亲和的完整 `WidgetPlacement`。
- [x] 保存完整位置。
- [x] 使用同一个目标显示器应用吸附边界。
- [x] 返回规范化位置给前端。
- [x] 恢复路径按亲和匹配显示器，并保证缺屏时可见。
- [x] 展开/收起时保持同一目标显示器，不重新回到主屏。
- [x] 将命令层收敛到薄的 finalize 命令。
- [x] 删除被替代的重复布局入口。
- [x] 更新 `bootstrap.rs` 命令注册。
- [x] 为错误添加上下文，能够区分窗口读取、显示器枚举、保存与应用边界失败。

退出条件：

- [x] 生产路径中只有 Rust 决定拖动结束的目标显示器。
- [x] 运行时 smoke 证明 finalize 返回并重新加载同一规范化位置。
- [x] `commands/*` 没有承载业务算法。

## 20. 阶段 4：前端收敛与权限清理

- [x] 在 runtime gateway 增加 `finalizeWidgetDrag` IPC 封装。
- [x] 解析 Rust 返回的完整 `WidgetPlacement`。
- [x] 删除前端目标显示器选择函数。
- [x] 删除 `availableMonitors`、`currentMonitor`、`primaryMonitor` 导入与映射代码。
- [x] controller 只管理拖动代次、稳定等待和 IPC 结果。
- [x] use hook 采用 Rust 返回的位置更新本地状态。
- [x] `WidgetShell` 调用签名无需调整，交互保持不变。
- [x] 更新浏览器 smoke 的 Tauri stub 与命令断言。
- [x] 检查 `src-tauri/capabilities/widget.json`，移除前端不再需要的 monitor 权限。
- [x] 运行 IPC contract 检查，94 个前端调用与 94 个注册命令一致。

退出条件：

- [x] 前端代码不再决定目标显示器。
- [x] 无死代码、无多余 monitor 权限、无新旧 IPC 并存造成的模糊所有权。

## 21. 阶段 5：合成多显示器矩阵

### 21.1 分辨率集合

- [x] 1280×720
- [x] 1366×768
- [x] 1600×900
- [x] 1920×1080
- [x] 2560×1440
- [x] 3840×2160

### 21.2 缩放集合

- [x] 100%
- [x] 125%
- [x] 150%
- [x] 200%

### 21.3 布局集合

- [x] 副屏位于右侧。
- [x] 副屏位于左侧并产生负 X。
- [x] 副屏位于上方并产生负 Y。
- [x] 副屏位于下方。
- [x] 两屏顶部不对齐。
- [x] 两屏之间有坐标间隙。
- [x] 工作区偏移与宽高收缩覆盖顶部、底部、左侧、右侧任务栏语义。

### 21.4 行为集合

- [x] A → B 拖动。
- [x] B → A 拖动。
- [x] 吸附左边。
- [x] 吸附右边。
- [x] `anchorY = 0`。
- [x] `anchorY = 0.28`。
- [x] `anchorY = 0.5`。
- [x] `anchorY = 1`。
- [x] 收起态拖动后展开保持亲和。
- [x] 展开后收起保持亲和。
- [x] 保存后重启恢复。
- [x] 目标屏断开后的安全回退。
- [x] 目标屏恢复后的亲和匹配。

### 21.5 必须独立覆盖的 Issue #55 组合

- [x] 主屏 150%，副屏 125% 的合成组合。
- [x] 主屏 → 副屏的合成选择。
- [x] 副屏 → 主屏的合成选择。
- [x] 左右吸附由 432 个边界矩阵案例覆盖。
- [x] Tauri 运行时验证保存值中的 monitor、side、anchorY。
- [x] Tauri 运行时验证保存后重新加载一致。

### 21.6 矩阵执行策略

完整笛卡尔积会产生大量重复案例。应采用：

- [x] 纯函数参数化测试覆盖全部 6 种分辨率 × 4 种缩放组合。
- [x] 空间布局测试独立于 DPI，并由 Issue #55 混合 DPI 组合交叉覆盖。
- [x] Issue #55 合成路径覆盖主副屏双向。
- [x] 对 Issue #55 原始组合执行领域选择与真实 Tauri 单屏持久化路径测试。
- [x] Rust 断言失败会输出输入矩形、候选显示器和最终选择。

退出条件：

- [x] 所有分辨率与缩放值至少参与一条自动化案例。
- [x] 负坐标、间隙、错位、任务栏与混合 DPI 均有独立断言。
- [x] Issue #55 组合的合成测试全通过。

## 22. 阶段 6：单屏运行时验证与双屏验收

### 22.1 当前机器可完成的运行时验证

- [x] 运行 `npm run test:tauri-runtime-smoke`。
- [x] 验证新命令已注册且 IPC 可调用。
- [x] 验证实际 `outer_position` / `outer_size` 读取路径。
- [x] 验证 SQLite 单键保存和重新加载。
- [x] 验证单屏恢复与工作区夹紧。
- [x] 验证缺失显示器亲和时安全回退。
- [ ] 运行 `npm run tauri dev` 做单屏人工拖动、展开、收起和重启 smoke。
- [x] 记录单屏测试不能证明真实 DWM 跨屏事件顺序这一限制。

### 22.2 不可作为正式证明的方法

- [x] 不把 Windows 虚拟桌面当作第二块显示器。
- [x] 不仅通过修改浏览器 viewport 声称跨屏通过。
- [x] 不仅通过截图中位置正确声称混合 DPI 通过。
- [x] 未采用虚拟显示驱动冒充真实双屏。

### 22.3 真实双屏验收协议

准备可测试构建后，请维护者或 Issue #55 报告者执行：

- [ ] Windows 显示设置：主屏 150%，副屏 125%。
- [ ] 记录两块屏幕分辨率、相对位置和任务栏位置。
- [ ] 启动 Patina，确认挂件初始可见。
- [ ] 主屏 → 副屏拖动并释放，确认不回弹。
- [ ] 副屏左边释放，确认吸附在副屏左边。
- [ ] 副屏右边释放，确认吸附在副屏右边。
- [ ] 在副屏展开挂件，确认仍以副屏为目标且完整可见。
- [ ] 收起后再次展开，确认不跳屏。
- [ ] 重启应用，确认恢复到副屏预期位置。
- [ ] 副屏 → 主屏重复上述步骤。
- [ ] 将副屏改到主屏左侧，重复双向拖动。
- [ ] 如可行，将一块屏调整为 100% 或 200%，补充一次混合 DPI 验证。
- [ ] 提供每一步结果、失败方向、释放位置与必要录屏。

退出条件：

- [ ] 真实双屏 150%/125% 双向拖动、展开/收起与重启恢复全部通过。
- [x] 已保留约束：若真机失败，先加入最小自动化复现，不凭猜测叠加延时。

## 23. 对抗式审查

实现与常规测试完成后，由审查者站在“该修复仍然有错”的立场逐项攻击。

### 23.1 所有权攻击

- [x] 搜索全部显示器选择调用，确认拖动结束路径只有一个决策点。
- [x] 搜索 `current_monitor`、`primary_monitor`、`available_monitors`；剩余 current/primary 只属于缺屏恢复回退，finalize 只枚举一次 available monitors。
- [x] 搜索 `cmd_apply_widget_layout`、`cmd_set_widget_placement` 和 `widget_placement_v2`，确认生产代码中均不存在。
- [x] 领域红测证明最终窗口矩形位于目标屏时不依赖源屏事件状态。

### 23.2 坐标与 DPI 攻击

- [x] 交换主副屏坐标与 DPI，结果仍正确。
- [x] 将副屏移至负 X、负 Y，结果仍正确。
- [x] 让窗口恰好一半跨屏，按中心与稳定键确定选择。
- [x] 让屏幕之间存在间隙，按到工作区最短距离选择。
- [x] 让工作区比挂件矮，使用饱和减法且 `anchorY = 0`。
- [x] 检查所有 logical/physical 转换点，确认位置判断全程物理像素，窗口尺寸仅按目标屏比例转换一次。

### 23.3 时序攻击

- [x] release 后注入延迟 moved 事件；审查中发现程序吸附 move 可能二次 finalize，已修复并加入回归断言。
- [x] 连续快速拖动两次，使第一次 IPC 晚于第二次返回。
- [x] finalize 期间切换展开状态时旧结果不会覆盖当前状态。
- [x] finalize 期间隐藏或销毁挂件窗口时旧结果被忽略。
- [x] 保存失败会中止吸附；set bounds 失败保留已保存位置并返回明确阶段错误。

### 23.4 持久化攻击

- [x] 数据库只有旧键时，确认不兼容读取。
- [x] 新 JSON 截断、类型错误、未知字段、非法数值时不 panic。
- [x] 新键 upsert 与旧键删除位于同一 SQLite 事务，失败不会提交半状态。
- [x] 断开目标屏启动时，不覆盖原亲和。
- [x] 重新接回目标屏时，亲和能再次匹配。

### 23.5 边界与维护性攻击

- [x] 命令处理器保持薄。
- [x] 没有将平台类型泄漏进领域纯函数。
- [x] 没有新增不必要的 shared/platform 通用层。
- [x] 没有 UI、主窗口或其他功能的顺带修改。
- [x] 测试验证行为而不是生产函数源码文本。
- [x] 每个关键失败都能从错误上下文定位到阶段。

退出条件：

- [x] 所有软件层攻击项通过；唯一发现的延迟 move 重入风险已修复并新增回归测试。
- [x] 代码审查未发现会把 finalize 选定目标重新覆盖为源屏的合法路径。

## 24. 全量质量门

最低门：

- [x] `npm test`
- [x] `npm run test:replay`
- [x] `npm run build`

本次边界与运行时变更附加门：

- [x] `npm run test:interaction`：12 passed。
- [x] `npm run test:ui-browser-smoke`：50 passed，挂件矩阵 144 cases。
- [x] `npm run test:tauri-runtime-smoke`：通过真实 Tauri 命令、事件、SQLite 与 capability smoke。
- [x] `npm run check:ipc-contracts`：94 / 94。
- [x] `npm run check:architecture`。
- [x] `npm run check:rust`：486 passed / 1 ignored，clippy 无 warning。
- [x] `npm run check`：全通过。
- [x] `npm run check:full`：ESLint 10 升级后全通过；npm 漏洞为 0，Rust 的 Windows 可达漏洞为 0。
- [x] `git diff --check`

结果处理：

- [x] 不静默豁免失败。
- [x] 依赖审计告警已通过兼容的 ESLint 10 成对升级消除。
- [x] capability schema 变化由标准 Tauri 命令生成并单独审查。
- [x] 查看最终 diff，确认没有调试日志、临时延时或测试专用生产分支。

## 25. 回滚方案

回滚的目标是恢复上一版稳定行为，而不是保留一半新契约。

- [x] 回滚约束已定义：同时回滚领域类型、IPC、运行时协调、前端网关和权限变更。
- [x] 回滚约束已定义：旧版本会忽略独立的 `widget_placement`，无需修改数据库 schema。
- [x] 回滚约束已定义：不为回滚额外恢复旧键双写。
- [x] 回滚约束已定义：回滚前记录触发原因、失败组合和日志。
- [x] 回滚约束已定义：回滚后重新运行最低质量门。

## 26. 建议提交拆分

每次提交前执行 `git diff --cached --stat` 与 `git diff --cached --numstat`。手工维护内容超过 1,000 行或超过 25 个文件时必须继续按所有权拆分。

### Commit 1：领域与存储（`90215fd`）

- [x] 领域位置模型、目标选择纯函数及测试。
- [x] 单一 `widget_placement` repository 与测试。
- [x] 已提交：`fix(widget): model monitor-aware placement (Refs #55)`

### Commit 2：Rust 完成事务（`a452a01`）

- [x] app 用例、薄命令、注册与运行时测试。
- [x] 已提交：`fix(widget): finalize drag on one target monitor (Refs #55)`

### Commit 3：前端收敛（`1c0fe83`）

- [x] gateway、controller、hook、capability 与前端测试。
- [x] 已提交：`refactor(widget): centralize drag finalization in Rust (Refs #55)`

### Commit 4：自动化验收与待真机文档

- [x] changelog、自动化证据、对抗式审查结果与真机阻塞状态。
- [ ] 文档归档：等待真实双屏验收后执行。

## 27. GitHub Project 协作

根据仓库规则，实施者报告状态建议，维护者在 Board 视图手动拖动。

- [x] 当前事项应保持 `In progress`，因为已进入方案与实施阶段。
- [x] 2026-07-26 在自动化完成 checkpoint 重新读取 live Project。
- [x] 自动化全部通过但等待真实双屏验收时，建议维护者将本事项从 `In progress` 拖到 `Blocked`。
- [x] 报告阻塞原因：“等待 150%/125% 真实双屏双向验收”，而不是把它误标为开发未完成。
- [ ] 真机验收通过后，建议维护者将本事项从 `Blocked` 拖到 `Done`。
- [x] 已重算 `Next`：若本事项改为 `Blocked`，建议将“复测并收口灵动视效”从 `Queued` 拖到 `Next`。
- [x] 不以本地清单、commit、push 或 Issue 状态代替 live Project 状态。

## 28. 完成定义

只有以下全部满足，任务才算彻底完成：

- [x] 根因红测已建立并转绿。
- [x] `WidgetPlacement` 完整包含显示器亲和、side 与 anchorY。
- [x] 只读写 `widget_placement`，不存在旧键兼容路径或双写。
- [x] 拖动完成只由 Rust 选择一次目标显示器。
- [x] 保存和吸附使用同一个目标显示器。
- [x] 合成分辨率、缩放、布局和行为矩阵全部通过。
- [x] Issue #55 的 150%/125% 组合自动化通过。
- [x] 单屏 Tauri 运行时 smoke 通过。
- [ ] 真实双屏 150%/125% 双向验收通过。
- [x] 全量代码质量门与依赖审计全部通过。
- [x] 对抗式审查完成且没有未关闭的高风险发现。
- [x] `CHANGELOG.md` 已加入准确的 `Refs #55`。
- [x] 四组实现与验证提交均保持可审查范围，并已推送到 `origin/main`。
- [ ] live Project 已由维护者拖至 `Done`，并重新核对 `Next` 窗口。

## 29. 归档清单

- [x] 将本文档中的执行项更新为真实结果，真实双屏、归档与 Project 人工操作保持未勾选。
- [ ] 在文档顶部补充完成日期、最终提交和真机验收证据。
- [ ] 将文件从 `docs/working/` 移至 `docs/archive/`。
- [ ] 确认顶层 `docs/` 未遗留一次性执行计划。
- [x] 运行 `git diff --check`。
- [x] 最终交付将说明：实现结果、验证范围、对抗式审查结果、Project 手动拖动要求和未归档原因。

## 30. 最终签字

- [x] 实施者：代码与自动化完成。
- [x] 审查者：对抗式审查完成。
- [ ] 真机验收者：混合 DPI 双屏验收完成。
- [ ] 维护者：Project 状态与 `Next` 队列已手动更新。
- [ ] 归档者：执行方案已移动到 `docs/archive/`。
